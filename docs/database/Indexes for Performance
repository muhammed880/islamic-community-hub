// Users
db.users.createIndex({ email: 1 }, { unique: true })
db.users.createIndex({ username: 1 }, { unique: true })
db.users.createIndex({ role: 1 })

// Masjids
db.masjids.createIndex({ registrationNumber: 1 }, { unique: true })
db.masjids.createIndex({ authority: 1 })
db.masjids.createIndex({ city: 1, state: 1 })

// Jobs
db.jobs.createIndex({ masjid: 1 })
db.jobs.createIndex({ status: 1 })

// Matrimony
db.matrimony.createIndex({ userId: 1 })
db.matrimony.createIndex({ gender: 1, agePreference: 1 })

// Donations
db.donations.createIndex({ donorId: 1 })
db.donations.createIndex({ masjid: 1 })
db.donations.createIndex({ createdAt: -1 })

// Quran Classes
db.quran_classes.createIndex({ imam: 1 })
db.quran_classes.createIndex({ masjid: 1 })

// Ask Imam Sessions
db.ask_imam_sessions.createIndex({ imam: 1 })
db.ask_imam_sessions.createIndex({ scheduledDate: 1 })

// Transactions
db.transactions.createIndex({ transactionId: 1 }, { unique: true })
db.transactions.createIndex({ fromUserId: 1 })
db.transactions.createIndex({ createdAt: -1 })
