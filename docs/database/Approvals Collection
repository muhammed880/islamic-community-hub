{
  _id: ObjectId,
  approvalId: String (unique),
  
  // What is being approved
  approvalType: Enum['masjid_registration', 'nikah_nama', 'needy_person', 'matrimony', 'donation'],
  referenceId: ObjectId,
  
  // Who is requesting approval
  requestedBy: ObjectId (ref: users),
  
  // Approval Details
  status: Enum['pending', 'approved', 'rejected'],
  approvedBy: ObjectId (ref: users - super_admin),
  approvalDate: Date,
  rejectionReason: String,
  comments: String,
  
  // Documents for verification
  documents: [String], // URLs
  
  // Timestamps
  requestedAt: Date,
  createdAt: Date,
  updatedAt: Date
}
