{
  _id: ObjectId,
  logId: String (unique),
  
  // User
  userId: ObjectId (ref: users),
  
  // Activity
  action: String, // 'login', 'create', 'update', 'delete', 'approve', etc.
  entityType: String, // 'user', 'masjid', 'donation', etc.
  entityId: ObjectId,
  
  // Details
  changes: Object, // What changed
  ipAddress: String,
  userAgent: String,
  
  // Timestamps
  timestamp: Date,
  createdAt: Date
}
